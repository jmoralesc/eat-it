.container
  .row
    .col-md-4
      .form-inline
        .form-group
          = form_tag(recipes_path, :method => "get") do
            = text_field_tag :search, params[:search], placeholder: "Ingredientes"
            %button.btn.btn-success{:type => "submit"} Buscar
